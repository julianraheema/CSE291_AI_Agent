{
  "id": "quickmap-california-roadconditions-screenshot",
  "snapshot": "chrome",
  "instruction": "Open the terminal by pressing Ctrl+Alt+T, then type google-chrome https://quickmap.dot.ca.gov/ and press HOTKEY ENTER. After waiting for the page to load, click “Options,” then click “Road Conditions.” Enable the checkboxes for “Fast,” “Lane closures,” “Full Closures,” and “CHP Incidents,” waiting briefly after each selection. Next, click “Zoom to,” choose “San Diego,” and wait for the map to update. Press F11 to enter full-screen mode, wait a moment, then press Alt+Print to capture a screenshot, and wait again to complete the action.",
  "source": "user",

  "config": [
    {
      "type": "launch",
      "parameters": {
        "command": [
          "google-chrome",
          "--remote-debugging-port=1337",
          "https://quickmap.dot.ca.gov/"
        ]
      }
    }
  ],

  "trajectory": "trajectories/quickmap-california-roadconditions-screenshot",

  "related_apps": [
    "chrome",
    "os",
    "terminal",
    "gnome"
  ],

  "evaluator": {
    "func": "infeasible"
  },

  "human-ground-truth": {
    "single-action": [
      "`HOTKEY` 'Ctrl+Alt+T'",
      "`TYPING` google-chrome https://quickmap.dot.ca.gov/",
      "`PRESS` Enter",
      "`WAIT` 2500",
      "`CLICK` 'Options'",
      "`CLICK` 'Road Conditions'",
      "`CLICK` 'Fast' checkbox",
      "`WAIT` 200",
      "`CLICK` 'Lane closures' checkbox",
      "`WAIT` 200",
      "`CLICK` 'Full Closures' checkbox",
      "`WAIT` 200",
      "`CLICK` 'CHP Incidents' checkbox",
      "`WAIT` 300",
      "`CLICK` 'Zoom to'",
      "`CLICK` 'San Diego'",
      "`WAIT` 2000",
      "`HOTKEY` 'F11'",
      "`WAIT` 1000",
      "`HOTKEY` 'Alt+Print'",
      "`WAIT` 1000",
      "`DONE`"
    ],

    "grouped-action": [
      [
        "`HOTKEY` 'Ctrl+Alt+T'",
        "`TYPING` google-chrome https://quickmap.dot.ca.gov/",
        "`PRESS` Enter",
        "`WAIT` 2500"
      ],
      [
        "`CLICK` 'Options'",
        "`CLICK` 'Road Conditions'"
      ],
      [
        "`CLICK` 'Fast' checkbox",
        "`WAIT` 200",
        "`CLICK` 'Lane closures' checkbox",
        "`WAIT` 200",
        "`CLICK` 'Full Closures' checkbox",
        "`WAIT` 200",
        "`CLICK` 'CHP Incidents' checkbox",
        "`WAIT` 300"
      ],
      [
        "`CLICK` 'Zoom to'",
        "`CLICK` 'San Diego'",
        "`WAIT` 2000"
      ],
      [
        "`HOTKEY` 'F11'",
        "`WAIT` 1000",
        "`HOTKEY` 'Alt+Print'",
        "`WAIT` 1000",
        "`DONE`"
      ]
    ]
  }
}
